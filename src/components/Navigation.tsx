import { Link } from "react-router-dom";

export const Navigation = () => {
  return (
    <header className="fixed top-0 left-0 right-0 h-[var(--nav-height)] flex items-center px-8 bg-background/80 backdrop-blur-sm z-50">
      
    </header>
  );
};
